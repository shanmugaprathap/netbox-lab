apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "netbox-lab.fullname" . }}-media
  labels:
    {{- include "netbox-lab.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.netbox.persistence.media.storageClass }}
  storageClassName: {{ .Values.netbox.persistence.media.storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.netbox.persistence.media.size }}
